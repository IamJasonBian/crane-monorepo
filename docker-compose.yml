version: "3.8"

services:
  # ─── Infrastructure ──────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ─── Crane Services ──────────────────────────────────────────────────
  crane-feed:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      redis:
        condition: service_healthy
    environment:
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      COUNTDOWN_API_KEY: ${COUNTDOWN_API_KEY}
      BESTBUY_API_KEY: ${BESTBUY_API_KEY}
      SLACK_WEBHOOK_URL: ${SLACK_WEBHOOK_URL}
    restart: unless-stopped

  crane-engine:
    build:
      context: .
      dockerfile: crane-engine/Dockerfile
    depends_on:
      redis:
        condition: service_healthy
      crane-feed:
        condition: service_started
    environment:
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      ENGINE_CONSUMER_GROUP: crane-engine
    restart: unless-stopped

  crane-manager:
    build:
      context: .
      dockerfile: crane-manager/Dockerfile
    depends_on:
      redis:
        condition: service_healthy
    ports:
      - "8000:8000"
    environment:
      REDIS_HOST: redis
      REDIS_PORT: "6379"
    restart: unless-stopped

  # ─── Legacy Scala service (optional) ─────────────────────────────────
  market-data-service:
    build:
      context: ./market-data-service
      dockerfile: Dockerfile
    depends_on:
      redis:
        condition: service_healthy
    environment:
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      ALPACA_KEY_ID: ${ALPACA_KEY_ID}
      ALPACA_SECRET_KEY: ${ALPACA_SECRET_KEY}
    profiles:
      - legacy
    restart: unless-stopped

volumes:
  redis-data:
